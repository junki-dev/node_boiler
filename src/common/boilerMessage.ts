export enum boilerError {
  // common error message
  INVALID_PARAMETER = 'invalid parameter',
  UNKOWN_ERROR = 'unkown error',

  // db error message
  DB_CONNECTION_ERROR = `couldn't connect to mongodb server`,
}
